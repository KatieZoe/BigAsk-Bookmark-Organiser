<!DOCTYPE html>
<html>
  <head>
    <title>BigAsk</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- Nav bar with Bootstrap formatting -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">BigAsk</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse " id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <!-- IF/ELSE Statement for display of sign in/login links or logout/new category links depending on status of user -->
        <% if @current_user.present? %>
            <li class="nav-item">
              <%= link_to "New Category", new_category_path, :class => 'nav-link' %>
            </li>
            <li class="nav-item">
              <a class="nav-link" <%= link_to 'Logout', login_path, :method => 'delete' %></a>
            </li>
            <li class="nav-item">
        <% else %>
              <a class="nav-link" <%= link_to 'Sign up', new_user_path %></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" <%= link_to 'Sign in', login_path %></a>
            </li>
        <% end  %>
      </div>
    </nav>
  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>
