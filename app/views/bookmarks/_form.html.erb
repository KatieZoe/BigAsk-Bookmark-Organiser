
<%= form_for @bookmark do |b| %>

<%= b.label :name %>
<%= b.text_field :name %>

<%= b.label :url %>
<%= b.text_field :url %>

<%= b.label "Add to category" %>
<%= b.select :category_id, @current_user.categories.pluck(:name, :id), :include_blank => true %>

<%= b.submit 'Submit' %>

<% end  %>
