<h1>Index </h1>


<% @categories.each do |category| %>
  <div class="category">
    <h4>
  <%= link_to category.name, category %>
        <li><% category.bookmarks.each do |bookmark|%>
          <%= bookmark.name %>
        <% end %>
        </li>
  </h4>
  </div>
<% end  %>
