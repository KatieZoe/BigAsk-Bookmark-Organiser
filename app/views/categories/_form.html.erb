
<%= form_for @category do |c| %>

<%= c.label :name %>
<%= c.text_field :name %>

<%= c.select :sort_option, category.pluck(:sort_option), :include_blank => true %>

<%= c.submit 'add category' %>


<% end  %>
