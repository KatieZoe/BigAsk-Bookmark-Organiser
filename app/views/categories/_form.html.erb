
<%= form_for @category, :class => 'mb-3' do |c|%>

<%= c.label :name, :class => 'form-label'%>
<%= c.text_field :name, :class => "form-control"%>

<%= c.select :sort_option, ['A to Z', "Newest to Oldest"]%>

<%= c.submit 'Submit' %>


<% end  %>
